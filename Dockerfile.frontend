# syntax=docker/dockerfile:1
FROM node:20-alpine

WORKDIR /app

# Install dependencies (use repo root context; copy only frontend manifests)
COPY src/frontend/package*.json ./
RUN npm ci || npm install

EXPOSE 5173

CMD ["npm", "run", "dev"]


